apiVersion: apps/v1
kind: Deployment
metadata:
  name: markdown-editor
  labels:
    app: markdown-editor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: markdown-editor
  template:
    metadata:
      labels:
        app: markdown-editor
    spec:
      containers:
        - name: markdown-editor
          image: registry.digitalocean.com/kubeden/markdown-editor:4ed87a7523e188f45106c3426c9e54b383168bc1
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: markdown-editor-secrets
                  key: database-url
            - name: DATABASE_SCHEMA
              valueFrom:
                configMapKeyRef:
                  name: markdown-editor-config
                  key: database-schema
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 5
      imagePullSecrets:
        - name: kubeden
